{% extends 'base.html.twig' %}

{% block title %}Plots{% endblock %}
{% block body %}
    hello {{ app.user.firstName }}
    <table border="1px">
    <th>Name</th>
        {% for plot in plots %}
            <tr>
                <td><a href='/plot/{{plot.id}}'>{{plot.name}}</a></td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}