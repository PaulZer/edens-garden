{% extends 'base.html.twig' %}

{% block title %}Plants{% endblock %}
{% block body %}
    hello {{ app.user.firstName }}
    <table border="1px">
    <th>Name</th>
    <th>Latin Name</th>
        {% for plant in plants %}
            <tr>
                <td>{{plant.name}}</td>
                <td>{{plant.latinName}}</td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}