{% block user_header %}
    <a class="btn btn-success {% if app.request.pathInfo == '/' %} active{% endif %}" href="{{ path('index') }}"><i class="fab fa-pagelines"></i> JARDINS</a>
    <a class="btn btn-success {% if app.request.pathInfo == '/account' %} active{% endif %}" href="{{ path('account') }}"><i class="fas fa-user-alt"></i> COMPTE</a>
    <a class="btn btn-success" href="{{ path('logout') }}"><i class="fas fa-sign-out-alt"></i> DECONNEXION</a>
{% endblock %}