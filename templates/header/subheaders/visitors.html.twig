{% block visitor_header %}
    <a class="btn btn-success" href="{{ path('login')}}"><i class="fas fa-user-plus"></i> INSCRIPTION</a>
    <a class="btn btn-success" href="{{ path('login')}}"><i class="fas fa-sign-in-alt"></i> CONNEXION</a>
{% endblock %}